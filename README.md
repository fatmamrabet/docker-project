# Docker Project

This project runs a Docker Compose stack with:

- nginx
- postgres

## How to run

1. Install Docker and Docker Compose
2. Run `docker-compose up -d`
3. Access Nginx at http://localhost:8080